# Контекст проекта — система автопротоколирования совещаний

## Назначение

Сервис преобразует аудиозаписи совещаний на русском, казахском и смешанной речи
в структурированный протокол. Первый модуль отвечает только за транскрипцию.

## Статус

- [x] Реализована транскрипция аудиофайла через OpenAI `whisper-1`
- [x] Добавлен provider/fallback слой diarization
- [x] Добавлен структурированный RU/KZ demo-fixture без внешних аккаунтов
- [x] Обработаны отсутствующий ключ и ошибки входного аудиофайла
- [x] Пакет устанавливается через `pyproject.toml` без ручного `PYTHONPATH`
- [x] Добавлено контекстное сопоставление имён без биометрии
- [x] Добавлено извлечение поручений со строгой схемой и проверкой цитаты
- [x] Добавлено краткое управленческое саммари
- [x] Добавлена сборка единого объекта протокола
- [x] Добавлен презентабельный DOCX-экспорт
- [x] Добавлен минимальный Flask UI
- [x] Полный demo-flow проходит без ключа до скачиваемого DOCX
- [x] README описывает чистую установку и пошаговую проверку для жюри
- [x] Добавлена безопасная раздельная диагностика ошибок diarization и fallback API
- [x] Исправлена безопасная speaker-нормализация и контекстное сопоставление имён
- [x] Добавлен опциональный LOCAL_KZ provider на
  `shyngys879/kazakh-whisper-large-v3-turbo`
- [x] LOCAL_KZ использует 30-секундный text-only pipeline, FP16 CUDA и CPU fallback
- [x] UI явно сообщает об отсутствии локальной diarization и облачном downstream

## Ограничения

- LOCAL_KZ проверен на одном предоставленном казахском аудио, но не на
  репрезентативном RU/KZ mixed корпусе.
- API-режим передаёт аудио во внешний облачный сервис и не является закрытым контуром.
- Причина отказа конкретного пользовательского аудио определяется по серверному логу;
  проверочные WAV-запросы к обоим OpenAI audio providers проходят успешно.
- Demo-режим использует явно обозначенные fixture/mock-данные.
- Контекстное имя назначается только при сильном сигнале; иначе остаётся `SPEAKER_XX`.
- В LOCAL_KZ нет локальной diarization: весь текст относится к `SPEAKER_00`.
- LOCAL_KZ оставляет аудио локально, но поручения и саммари всё ещё используют
  облачный OpenAI API; полный on-premise pipeline пока не реализован.
- Optional LOCAL_KZ environment и модельный cache существенно тяжелее базовой установки.

## Проверено

- Чистая установка в новой `.venv` по `requirements.txt`.
- Импорт установленного пакета без ручного `PYTHONPATH`.
- 35 автоматических тестов без платных API-вызовов и без загрузки локальной модели.
- HTTP demo-flow: стартовая страница, обработка, результат и скачивание DOCX.
- DOCX структурно проверен и визуально просмотрен после рендера.
- Demo-mode работает без API-ключа и сети.
- Benchmark LOCAL_KZ: RTX 3050 Laptop, аудио 78,43 с, FP16 CUDA inference 15,34 с,
  model cache около 1,51 ГБ.
- Интегрированный LOCAL_KZ provider повторно обработал это аудио на `cuda:0` из
  offline Hugging Face cache без загрузки модели из сети.
